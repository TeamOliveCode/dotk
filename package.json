{
  "name": "dotk",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.21.0",
  "scripts": {
    "build": "turbo build",
    "bundle": "turbo build && node scripts/bundle.js",
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "turbo lint"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  }
}
